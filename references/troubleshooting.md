# Troubleshooting

- Config error: fill required `config.yaml` values.
- Locked Excel: close it and rerun; exporter creates a timestamped alternate.
- 429/captcha/login/paywall: stop that source and review later.
- Interrupted run: rerun command; SQLite preserves progress.
- Empty dynamic page: HTTP first, optional browser fallback only for thin JS pages.

